import 'package:flutter/material.dart';

class SearchWallpaperPage extends StatelessWidget {
  const SearchWallpaperPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
